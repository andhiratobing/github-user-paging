package submission.andhiratobing.githubuser.util.state

class ResourceState {

    companion object {
        val LOADING: ResourceState = ResourceState()
        val LOADED: ResourceState = ResourceState()
        val FAILED: ResourceState = ResourceState()

    }
}